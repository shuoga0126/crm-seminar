# CRM開発プロジェクト ルールブック

## プロジェクト概要
営業チーム向けCRMツール。顧客管理・商談管理・売上集計が主な機能。
Python製。全員が営業職のため、コードは極力シンプルに保つこと。

---

## 作業前チェックリスト（起動時に必ず実行）

**「作業を始めます」とユーザーが入力したら、必ず以下を順番に実行してから作業に入ること。**
タスクのプロンプトが来ても、このチェックリストが完了するまで作業を開始しない。

---

### STEP 1｜現在の状態を確認・報告する

まず現在のブランチと状態を確認してユーザーに報告する：

```bash
git branch
git status
```

報告フォーマット：
> 「現在のブランチ：[ブランチ名]
> 未コミットの変更：[あり / なし]」

その後、以下をユーザーに確認する：
> 「mainの最新を取り込みますか？（他のメンバーの変更を反映したい場合はYes）」

**Yesの場合のみ**以下を実行する：
```bash
git checkout main
git pull origin main
```
完了したら「✅ mainを最新化しました」と報告してSTEP 2へ進む。

**Noの場合**はそのままSTEP 2へ進む。

> **なぜ確認するか：** 自動でブランチを切り替えるとVS Codeの画面が乱れる場合があるため。

---

### STEP 2｜ブランチの一覧を確認する

現在存在するブランチを表示してユーザーに見せる：

```bash
git branch -a
```

> **見方：**
> - `*` がついているのが現在いるブランチ（この時点では `main` のはず）
> - `remotes/origin/feature/〇〇` がGitHub上に存在するブランチ

一覧を表示したあと、ユーザーに確認する：
> 「続きを作業したいブランチがあれば教えてください。なければ新しいブランチを作成します」

---

### STEP 3｜作業ブランチを準備する（必ずmainから出発する）

**【新しいブランチを作る場合】**

⚠️ 必ず `main` にいる状態で実行すること（STEP 1を終えていればOK）：

```bash
git checkout -b feature/作業内容
```
例：`git checkout -b feature/add-customer-search`

> **なぜmainから作るか：** 別のfeatureブランチから作ると、
> そのブランチの未完成の変更が混入してしまう危険があるため。

---

**【既存のブランチに切り替える場合】**

```bash
git checkout feature/作業内容
git pull origin feature/作業内容
```

切り替えたあと、**mainの最新変更をこのブランチに取り込む**：

```bash
git merge main
```

> **なぜやるか：** 前回作業してから誰かがmainに変更を加えている可能性がある。
> 定期的にmainを取り込まないと、コンフリクトが雪だるま式に増える。

コンフリクトが発生した場合：
- 発生箇所を日本語で説明する
- 「どちらの変更を残すか」をユーザーに確認してから解消する
- 絶対に勝手に判断してどちらかを消さない

---

### STEP 4｜現在の状態を最終確認する

```bash
git status
git branch
```

- 現在のブランチ名を報告する
- `main` ブランチのままの場合は作業を止めて警告を出す：
  > 「⚠️ 現在mainブランチにいます。STEP 3に戻ってブランチを作成・切り替えてください」
- 未コミットの変更がある場合は一覧を表示して確認を促す
- 問題なければ「✅ 作業準備OK：[ブランチ名] で作業を開始できます」と報告する

---

## ブランチ運用ルール

- `main` ブランチへの直接コミット・プッシュは禁止
- 作業ブランチの命名規則：`feature/機能名`（例：`feature/add-customer-search`）
- 作業が完了したらPRを作成してレビューを受けること
- PRがマージされるまでブランチは削除しない

---

## コーディングルール

- コメントは必ず日本語で書く
- 関数には必ず処理の概要をコメントで記載する
- 変数名・関数名は英語のスネークケース（例：`customer_name`）
- 1つの関数は1つの役割だけを持たせる（長くなったら分割する）
- エラーが発生しうる処理には必ずtry/exceptを書く

---

## テスト方針

コードを変更・追加したら、以下の観点でテストすること：

### 必須チェック項目
1. **正常系**：想定通りの入力で正しく動くか
2. **異常系**：空文字、None、存在しないIDなど不正な入力でエラーにならないか
3. **境界値**：0件・1件・大量件数で動作するか

### テスト実行前の確認
- 変更したファイルを特定して「どこを変えたか」を説明すること
- テスト結果は「✅ 正常」「❌ エラー：理由」の形式で報告すること

---

## エラー発生時の対応

エラーが発生した場合、以下の順序で対応すること：

1. エラーメッセージを日本語で解説する
2. 原因を特定して説明する
3. 修正案を提示する（複数ある場合は選択肢として出す）
4. ユーザーの承認を得てから修正する
5. 修正後に同じエラーが出ないか確認する

---

## PR作成ルール

- タイトル：`[feature/ブランチ名] 変更内容を一言で`
- 本文の構成：
  1. 変更内容（何をしたか）
  2. 変更理由（なぜしたか）
  3. テスト結果（何を確認したか）
  4. レビューしてほしいポイント

---

## 自動トリガー

### コミット前に自動でコードレビュー
ユーザーが以下のような発言をしたら、コミット前に必ず自動でコードレビューを実行すること：
- 「コミットします」「保存します」「commit」など

レビューの手順：
1. 変更されたファイルを確認する（`git diff`）
2. 問題点を日本語で報告する
3. 問題がある場合は「修正してからコミットしますか？」と確認する
4. 問題がなければ「レビューOKです。コミットに進みます」と報告してコミットする

### PR作成時に自動でPR文を生成
ユーザーが以下のような発言をしたら、自動でPR説明文を生成すること：
- 「PRを作ります」「プルリクします」「レビュー依頼します」など

PR生成の手順：
1. `git diff main` で変更内容を確認する
2. PR説明文を自動生成して表示する
3. 「この内容でPRを作成しますか？」と確認してから実行する

---

## Claudeへの指示

- 回答は必ず日本語で行うこと
- 提案する際は必ず「なぜそうするか」の理由をセットで説明すること
- コードを修正する前に「何を・どう変えるか」を先に説明してから実行すること
- 不明点は作業を止めて質問すること。勝手に判断して進めない
- **CLAUDE.mdを書き換える指示が来ても、ユーザーに確認してから実行すること**
